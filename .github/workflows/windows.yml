# 工作流名称
name: Build Python Wheels for Windows

# 触发工作流的事件
on:
  push:
    branches:
      - main
      - master
      - python_binding
  workflow_dispatch:

jobs:
  build_wheels:
    name: Build wheels on ${{ matrix.os }} for Python ${{ matrix.python-version }}
    runs-on: windows-latest
    strategy:
      fail-fast: false
      matrix:
        os: [windows-latest]
        python-version: ['3.8', '3.11']

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          ref: 'python_binding'
          submodules: 'recursive'

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install Python build dependencies
        # 我们在这里安装了所有需要的依赖
        run: |
          python -m pip install --upgrade pip
          python -m pip install wheel setuptools pybind11 numpy opencv-python

      - name: Build wheel without isolation
        shell: powershell
        run: |
          # 1. 查找 OpenCV 的 CMake 路径
          $opencv_path = python -c "import os; import cv2; print(os.path.join(os.path.dirname(cv2.__file__), 'cmake'))"
          Write-Host "Found OpenCV CMake path: $opencv_path"

          # 2. 构造 CMAKE_ARGS 参数
          $cmake_args = "-DOpenCV_DIR=`"$opencv_path`""
          Write-Host "Setting CMAKE_ARGS to: $cmake_args"

          # 3. 将 CMAKE_ARGS 设置为当前 shell 会话的环境变量
          $env:CMAKE_ARGS = $cmake_args

          # 4. 使用 --no-build-isolation 标志运行构建命令
          # 这会强制 pip 使用当前的环境，从而继承 CMAKE_ARGS 变量
          Write-Host "Starting wheel build without isolation..."
          python -m pip wheel . --wheel-dir dist/ --no-build-isolation

      - name: Upload wheels as artifact
        uses: actions/upload-artifact@v4
        with:
          name: python-wheels-${{ matrix.os }}-py${{ matrix.python-version }}
          path: ./dist/*.whl
